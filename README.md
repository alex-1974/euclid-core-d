# euclid-core-d

**Experimental local architecture probe.**

This repository is currently an internal shared-contract vehicle for testing
the planned `geo-d` / `geo3-d` architecture.

It is not yet a user-facing library and its name is not frozen.

Normal geometry consumers should use `geo-d` or `geo3-d`, not this package
directly.
